import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RAG-BOT: AI Knowledge Retrieval Chatbot by Amon",
  description: "RAG-BOT is an AI-powered chatbot built by Amon using Retrieval Augmented Generation (RAG) techniques. Instantly fetch, understand, and interact with custom documents and knowledge bases. Powered by Next.js, AstraDB, FastAPI, and LangChain.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
